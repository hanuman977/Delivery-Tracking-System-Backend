server:
  port: 8888

spring:
  application:
    name: "config-server"

  cloud:
    config:
      server:
        git:
          uri: "https://github.com/hanuman977/delivery-tracking-system-configs.git"
          search-paths: ""
          clone-on-start: true
          default-label: "main"